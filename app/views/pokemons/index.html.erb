<h1>Pokemons</h1>

<%# <ul id="list" class="list"></ul> %>

<div class="list">
  <% @pokemons.each do |pokemon| %>
    <div class="list-item">
      <%= pokemon.name %>
      <%= image_tag("#{pokemon.sprites}") %>
    </div>
  <% end %>
</div>

<% console %>
